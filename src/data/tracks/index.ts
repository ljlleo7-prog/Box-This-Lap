import { Track } from '../../types';
import { SILVERSTONE } from './silverstone';
import { MONZA } from './monza';
import { SPA } from './spa';

export const TRACKS: Track[] = [
    SILVERSTONE,
    MONZA,
    SPA
];

export { SILVERSTONE, MONZA, SPA };
